services:
  - type: web
    name: youtube-downloader-service
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    rootDir: youtube-service
    healthCheckPath: /health
